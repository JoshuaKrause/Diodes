/// Initialize grid.
draw_set_color(c_white);

gridSize = argument0;

show_debug_message(sprite_width);

cellWidth = room_width / gridSize;
cellHeight = room_height / gridSize;

show_debug_message(cellWidth);


